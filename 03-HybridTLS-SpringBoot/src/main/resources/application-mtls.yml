server:
  ssl:
    client-auth: NEED
    trust-store: ${SERVER_SSL_TRUST_STORE}
    trust-store-password: ${SERVER_SSL_TRUST_STORE_PASSWORD}
    trust-store-type: ${SERVER_SSL_TRUST_STORE_TYPE:PKCS12}
